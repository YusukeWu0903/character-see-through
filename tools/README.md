# Local helper tools

`cloudflared.exe` may be downloaded here for an explicitly requested temporary
Cloudflare Quick Tunnel. It is ignored by version control and is not required
for inference, PSD export, or local viewer use.
